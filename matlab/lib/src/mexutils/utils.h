//
// Created by ilario on 06/08/15.
//

#ifndef HASP_TRACKER_UTILS_H
#define HASP_TRACKER_UTILS_H

#include <sys/time.h>
#include <ctime>
#include <chrono>

/* Remove if already defined */
typedef std::chrono::high_resolution_clock Time;
typedef std::chrono::milliseconds ms;
typedef std::chrono::duration<double > lsec;

#endif //HASP_TRACKER_UTILS_H
